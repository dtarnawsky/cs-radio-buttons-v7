import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.radio.buttons.v7',
  appName: 'cs-radio-buttons-v7',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
